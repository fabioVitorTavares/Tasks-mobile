import { StyleSheet } from "react-native";

export const  { stylesHome } = StyleSheet.create({
  stylesHome: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  }
});