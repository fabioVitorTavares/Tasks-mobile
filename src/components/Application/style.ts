import { StyleSheet } from "react-native";

export const S = StyleSheet.create({
  application: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',   
    width: '100%',
    height: '100%'
  }
});